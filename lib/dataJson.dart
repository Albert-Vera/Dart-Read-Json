
class DataJson {

  var type_code, type_name, sphere_code, sphere_name, code, name;
  var traits, text, flavor;
  var ilustrator, octgnid, url, imagesrc;
  var position, threat, willpower, attack, defense, health, quantity, deck_limit;
  var is_unique, has_errata;

  DataJson(this.type_name, this.name, this.text, this.url, this.imagesrc);

}